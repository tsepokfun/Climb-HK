{
  "name": "climb-hk",
  "version": "1.0.0",
  "private": true,
  "description": "Hong Kong climbing spots - static bilingual site (zero-dependency).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node tools/validate-spots.mjs"
  }
}