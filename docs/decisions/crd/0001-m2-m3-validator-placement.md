# CRD 0001:M2/M3 任务归属与文档一致性修正

- **谁提出**:文档一致性检查子代理(只读核对报告,2026-09-08),PM 采纳
- **要什么**:修复任务表中 2 条 blocking 矛盾 + PRD/任务表 5 条 optional 笔误
- **为什么**:文档内部自相矛盾——M2 验收要求校验脚本产出,但校验脚本任务排在 M3;T-05 的工作描述触碰它不拥有的文件
- **触及**:PRD(里程碑 M2/M3 描述、目标编号、diff 示例、F-3 脚本路径)、`docs/tasks/tasks.md`(T-05 的里程碑归属与工作措辞、表头补「代码注释用英文」、T-01 DoD 补「+ 档位归并」说明)、状态文件(任务-里程碑映射)
- **成本**:无返工。T-01 正在跑,不受影响;T-05 尚未开始,只是归属与措辞调整
- **DoD 项目新增**:无(不新增工作,只调整归属)
- **决策**:accepted。2026-09-08 用户选定**方案 A**(校验脚本 T-05 移入 M2),附带修正一并执行;理由:快照对照是「全量数据」验收的一部分,脚本与数据必须同里程碑闭环。

## 方案 A(推荐):把校验脚本 T-05 移入 M2

- M2 = 难度筛选接线 + 首页接线 + 全量数据 + 校验脚本与快照对照(零缺失)
- M3 = 维护文档 + CI + README/CHANGELOG + QA 与评审
- 理由:快照对照是「全量数据」验收的一部分,脚本与数据必须同里程碑闭环;M3 的「维护与交付」仍有文档/CI/QA 支撑,结构不变
- 附带修正(两种方案都会做):T-05 工作描述删去「npm run validate 接入 package.json」(该脚本 T-01 已占位,T-05 只写 `tools/validate-spots.mjs`,不再触碰 package.json,解决所有权冲突)

## 方案 B:保持 T-05 在 M3,M2 的对照改为人工核对

- M2 DoD 改为:全量数据与快照「人工逐条对照」零缺失(T-05 完成后再转自动化)
- 代价:M2 验收少一道机器保障,全量数据先靠人肉核对,自动化推后

## Optional 修正(随选定方案一并执行,已记录于此)

1. PRD 目标编号笔误:「任务1/任务2/任务2」→「目标 1/目标 2/目标 3」,避免与 T-xx 混淆
2. PRD diff 示例补全:F2B、F7C+、Top Rope/Lead 实际出现的格式
3. 任务表表头补「代码注释用英文」
4. T-01 DoD 补明:V7+ 区间视为包含 V7 档(该语义已在下发给 T-01 的简报中要求)
5. PRD F-3 路径笔误:index.html 引用 `../js/spots-data.js` → `js/spots-data.js`(首页在根目录)

## Applied

- `docs/design/prd-2026-09-08-map-grade-filter-hk-spots.md` → v2
- `docs/tasks/tasks.md` → v2
- 状态文件 `~/.dsh/crew/jobs/map-grade-filter-hk-spots/state.json`:T-05 归入 M2,docs 版本更新,新增 crds 记录
